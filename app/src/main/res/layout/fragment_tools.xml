<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_fragment_rounded"
    android:clipToOutline="true">

    <LinearLayout
        android:id="@+id/tools_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingTop="12dp"
        app:layout_constraintTop_toTopOf="parent">

        <HorizontalScrollView
            android:id="@+id/most_used_bar"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:scrollIndicators="none"
            android:fadingEdge="horizontal"
            android:fadingEdgeLength="32dp"
            android:requiresFadingEdge="horizontal">

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:paddingStart="16dp"
                android:paddingEnd="8dp">

                <TextView android:id="@+id/mostT_1" style="@style/MostUsedPill" />
                <TextView android:id="@+id/mostT_2" style="@style/MostUsedPill" />
                <TextView android:id="@+id/mostT_3" style="@style/MostUsedPill" />
                <TextView android:id="@+id/mostT_4" style="@style/MostUsedPill" />

            </LinearLayout>
        </HorizontalScrollView>

        <ImageButton
            android:id="@+id/reorder_btn"
            style="?android:attr/borderlessButtonStyle"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginEnd="8dp"
            android:src="@drawable/ic_edit"
            app:tint="?attr/colorOnSurfaceVariant" />

    </LinearLayout>

    <com.ernestoyaquello.dragdropswiperecyclerview.DragDropSwipeRecyclerView
        android:id="@+id/tools_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tools_header" />

</androidx.constraintlayout.widget.ConstraintLayout>
